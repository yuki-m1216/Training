apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: dev-

replicas:
  - name: sample-app
    count: 1

patchesStrategicMerge:
  - deployment-patch.yaml

images:
  - name: nginx
    newTag: 1.21-alpine