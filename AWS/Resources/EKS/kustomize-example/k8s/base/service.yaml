apiVersion: v1
kind: Service
metadata:
  name: sample-app-service
  namespace: app-namespace
  labels:
    app: sample-app
spec:
  selector:
    app: sample-app
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP