apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: prod-

replicas:
  - name: sample-app
    count: 5

patchesStrategicMerge:
  - deployment-patch.yaml

images:
  - name: nginx
    newTag: 1.22-alpine