apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml

labels:
- pairs:
    app: sample-app

namePrefix: base-

images:
  - name: nginx
    newTag: "1.21"