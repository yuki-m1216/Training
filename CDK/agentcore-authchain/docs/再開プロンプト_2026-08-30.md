# 再開プロンプト（次回セッション冒頭に貼り付ける）— 2026-08-30 作成

以下をそのまま新しいセッションの最初のメッセージとして貼ってください。

---

AgentCore 認証チェーン検証（`CDK/agentcore-authchain/`、ブランチ `feat/agentcore-authchain-v0`）の続きを行います。以下の状態から再開してください。

## 目的（2 軸）
1. 要件固有：組織属性の正規化 → エージェント認可マスタ → `agents` クレーム → Runtime 入口認証 → 同一 Bearer 透過 → Gateway Policy（Cedar）の連鎖を実測で確かめる
2. 汎用：AgentCore（Runtime／Gateway／Identity／Policy／Observability）で AI エージェントシステムを組む型を、**公式ベストプラクティスに沿って**作りながら理解する。要件固有でない部分はベストプラクティス準拠、検証の都合で緩めた箇所は「簡略化」と明記して本番案を併記（`docs/整理_システム全体像_v1_0.md` §5）

## 現在地
- **V1' 完了**（OIDC 化、SAML 撤去、コミット `c58670d`）、**V2-1 完了**（`AgentEntitlement`＋`agents`／`upn` 注入、コミット `bad7168`）。両トークンに `agents: ["inspect-headers"]` を実測済み
- 整理 docs：`整理_SAMLとOIDC`、`ツール使い方_観測手順`、`索引`、`整理_システム全体像`（現在の構成の正本）を作成済み
- **V2a は未デプロイ**：`cdk/lib/runtime-stack.ts`（`AuthChainRuntimeStack`、JWT＋`customClaims: agents CONTAINS inspect-headers`、`-c forwardAuth=true` で `Authorization` 転送）は synth 済み。`runtime-code/build/` 生成済み（無ければ `runtime-code/build.sh`）
- **要注意（V2a の予想）**：Runtime は 2026-06-30 以降 MMDSv2 必須だが CFN スキーマに `MetadataConfiguration` が無く CDK で設定不可 → 最初の invoke が `ValidationException` になる予想。`aws bedrock-agentcore-control update-agent-runtime --agent-runtime-id <id> --metadata-configuration requireMMDSV2=true` で回復して再実測（`検証ログ_V2` チェックリスト #7、計画書 §6-8）
- すべてコミット済み（`git log -1` で確認）。**origin へ未 push**（push 前に秘密スキャン：GUID／12 桁／PoolId／26 文字クライアント ID／メール／JWT／鍵）
- 稼働リソース：`AuthChainIdentityStack`（Cognito＋DynamoDB×2＋Lambda）、Secrets Manager `authchain/entra-oidc-client-secret`（$0.40/月、期限 2027-02-25）。Entra 側：OIDC App registration（SAML エンタープライズアプリは残置）
- ローカルの秘密（gitignore）：`out/entra-oidc.json`、`out/identity-outputs.json`、`out/tokens_userA-oidc.json`（`agents`／`upn` 入り。60 分で期限切れ → 再ログイン）、`out/tokens_local-a.json`、`out/local-user-a.password`

## 最初に読むもの
1. `docs/索引_v1_0.md`（§0 目的、§1 読む順、§3 V 終了時チェックリスト）
2. `docs/整理_システム全体像_v1_0.md`（§1-2 現在の構成、§4 認可 3 層、§5-(b) 未デプロイ部分の方針）
3. `docs/検証ログ_V2_v1_0.md`（V2-1 の実測、チェックリスト #3〜#7）
4. `docs/計画_追加要件_v1_0.md` §3-3／§5／§6／§8-3
5. 必要に応じて `docs/ツール使い方_観測手順_v1_0.md`（`get_token.py`／`invoke_runtime.py`、セッション Cookie の罠）

## 今回やること（この順で）
1. **V2a：Runtime デプロイ（`forwardAuth` なし）**
   - `cdk diff AuthChainRuntimeStack` の予想（Runtime 1、実行ロール＋ポリシー、出力 4、クロススタック参照）→ 実測 → deploy（IAM を作るので事前確認）→ `--outputs-file ../out/runtime-outputs.json`
   - 再ログイン（`get_token.py --user userA-oidc --idp EntraOIDC`）→ `invoke_runtime.py --token-file out/tokens_userA-oidc.json`：予想は MMDSv2 の `ValidationException` → `update-agent-runtime` で有効化 → 200 で `received_header_names` に `Authorization` が**無い**
   - Runtime のログ群（`/aws/bedrock-agentcore/runtimes/<id>-DEFAULT`）と CloudTrail の記録を確認（Observability の現状把握）
2. **V2b：`-c forwardAuth=true`** で再デプロイ（diff は `RequestHeaderConfiguration` の 1 点）→ `Authorization` が届き JWT の要点（`agents` 等）が返る
3. **壊す実験**：`--tamper`、`--use id`（`client_id` 無し）、`out/tokens_userA_v1c_failclosed.json`（`company_code` 無し・期限切れ）、`AgentEntitlement` の行削除 → 再ログイン → `agents` 無しトークン → Runtime の HTTP コード（401 か 403）を記録。復元は `put-item`
4. 検証ログ V2 の完了サマリ（シーケンス図）、仮決め #13/#15、コスト表 V2 行、`整理_システム全体像` §1/§4/§5-(b)、再開プロンプトを更新 → コミット
5. 以降 V3（Gateway 透過）／V4（Policy）は計画書 §5・§8-4。V5（Strands＋モデル）は V4 後に検討

## 進め方のルール
- 核心ステップは「予想 → 実測 → 照合」（予想は Claude が代行可）。周辺ステップは質問せず進める。課金・削除・IAM・グローバル設定の変更は事前確認
- `export AWS_PROFILE="Y_admin"` は毎セッション確認。テストは書かない。一度に 1 変更。公開リポジトリのマスク規約（`<tenant>`/`<ACCOUNT>`/`<PoolId>`/`<ClientId>`/`<user>@<tenant>…`）
- 設計判断は「要件固有／汎用（ベストプラクティス準拠）／簡略化」を明示。公式推奨からの逸脱は理由付きで `整理_システム全体像` §5 に記録し、実装を変えるかは各 V の diff 提示時に個別確認
- `get_token.py` をバックグラウンドで使うなら `python3 -u`、停止は PID 指定。別ユーザーの検証はシークレットウィンドウか `/logout`

## 未確認事項（計画書 §6）
Runtime の customClaims 不一致時の HTTP コード／MMDSv2 の既定値／Policy での配列クレーム（`agents`）の tag 表現／フェデレーションユーザーの refresh 時 `triggerSource`／`CfnPolicy.enforcementMode` と policy 単位 `enforcementMode` の実動作／Gateway Lambda ターゲットに渡るユーザー情報

まず現状確認（`git status`、`git log --oneline -3`、`out/` の有無、`runtime-code/build/` の有無）をして、V2a の手順と `cdk diff` の予想を提示してください。
