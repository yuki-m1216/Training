# コスト見積りと撤収手順（下書き） v1.0

対象プロンプト：AgentCore認証チェーン検証実装 v1.1（§4 V0-6、§6）。リージョン ap-northeast-1。各 V の終了時に「稼働リソースと概算コスト」節を追記し、撤収時にチェックリストとして使う。

## 1. 単価（公式料金ページ、2026-08-16 取得）

| サービス | 単価 | 備考 |
|---|---|---|
| Cognito Essentials（直接サインイン） | 10,000 MAU/月まで無料、以降 $0.015/MAU | 無料枠は恒久 |
| Cognito Essentials（**SAML/OIDC フェデレーション**） | **50 MAU/月まで無料**、以降 $0.015/MAU | Entra 経由の User-A/B はこちら |
| Cognito M2M トークン | $0.00225/成功リクエスト | 本検証では M2M クライアントを作らない（0） |
| AgentCore Runtime（microVMs） | CPU $0.0895/vCPU-h、メモリ $0.00945/GB-h。**アイドル・I/O 待ちは無料**、1 秒単位 | 消費ベース。常時稼働課金なし |
| AgentCore Gateway | API 呼び出し（ListTools/InvokeTool/Ping）$0.005/1,000、Search $0.025/1,000、ツールインデックス $0.02/100 ツール月 | |
| AgentCore Identity | Runtime/Gateway 経由の利用は追加課金なし | |
| Policy in AgentCore | 認可リクエスト $0.000025/件、自然言語ポリシー生成 $0.13/1,000 トークン | 生成機能は使わない |
| AgentCore Observability | CloudWatch 料金に準拠 | |
| CloudWatch Logs（東京） | 取り込み約 $0.76/GB、保管約 $0.033/GB 月 | 数 MB 規模 |
| S3（CDK アセットバケット） | ストレージ $0.025/GB 月 | Runtime の直接コードデプロイ zip（V2、数十 MB）と Lambda zip。ECR は**使わない**（仮決め #13） |
| DynamoDB（オンデマンド） | 読み書きとも極小 | 認可マスタ（数件） |
| Lambda | 無料枠内 | Pre Token Gen／echo_profile |

## 2. 本検証の想定使用量と概算（V0〜V4 通し、約 2 週間）

| 項目 | 想定量 | 概算 |
|---|---|---|
| Cognito | フェデレーション 2 MAU＋ローカル 1 MAU | **$0**（無料枠内） |
| Runtime | 呼び出し 200 回 × 平均 2 秒 × 1 vCPU / 1 GB | 約 0.11 vCPU-h → **≈ $0.01** |
| Gateway | 呼び出し 300 回、ツール 1 本 | ≈ $0.0015 ＋ インデックス $0.02/月 → **≈ $0.02** |
| Policy | 認可 300 件 | **≈ $0.01** |
| CloudWatch Logs | 〜50 MB | **≈ $0.04** |
| S3 アセット | 〜0.1 GB × 0.5 月 | **≈ $0.00** |
| DynamoDB／Lambda／Identity | 無料枠・極小 | **≈ $0** |
| **合計** | | **$1 未満／月**（推定）。固定費は S3 アセットと Logs 保管のみ |

- 既存の兄弟プロジェクト（agentcore-gateway 3 スタック）は別途稼働中（今回は触らない。§0 決定）
- 想定外の課金要因になり得るもの：Runtime の長時間実行（エージェントがハングして CPU を食う）、Logs の DEBUG 大量出力、`cdk deploy` の反復による S3 アセット蓄積（極小。撤収時に `cdk gc` で掃除可）

## 3. 各 V 終了時の稼働リソース一覧（追記式）

| V | 稼働リソース | 一時リソース（要後始末） | 概算/月 |
|---|---|---|---|
| V0 | なし（AWS リソース未作成） | ローカル：`tonistiigi/binfmt` イメージ、一時 DOCKER_CONFIG（scratchpad） | $0 |
| V1（完了時） | `AuthChainIdentityStack`：UserPool（Essentials、SAML IdP EntraID、Pre Token Gen V2_0 接続済）／ドメイン／PKCE クライアント／DynamoDB（6 件）／Lambda×2／LogGroup×2 | ローカル：`out/` にトークン退避 4 ファイル・SAML 2 ファイル・パスワード・メタデータ URL（すべて gitignore）。DynamoDB の一時削除行は復元済み。プールに管理者アカウントの副産物ユーザー 1 名（撤収時に消える） | ≈ $0（フェデレーション 2 MAU・ローカル 1 MAU） |
| V1'（完了時、2026-08-30） | `AuthChainIdentityStack`：UserPool（Essentials、**OIDC IdP EntraOIDC のみ**。SAML IdP は削除済）／ドメイン／PKCE クライアント／DynamoDB（6 件）／Lambda×2／LogGroup×2、**Secrets Manager `authchain/entra-oidc-client-secret`（$0.40/月、有効期限 2027-02-25）** | ローカル：`out/` に OIDC トークン 2 ファイル（`tokens_userA-oidc.json`／`_v1b.json`）、`entra-oidc.json`（テナント ID／クライアント ID）、SAML 時代の遺物（`saml-metadata-url.txt`／`saml_*`／`tokens_userA*.json`。転記済みなので撤収時に削除）。プールは 2 名（`local-user-a`／`EntraOIDC_<sub>`。管理者副産物ユーザーは V1'-c で削除済） | ≈ $0.40（Secrets Manager のみ。Cognito はフェデレーション 1 MAU・ローカル 1 MAU で無料枠） |
| V2-1（identity 側のみ、2026-08-30） | V1' の稼働リソース＋ **DynamoDB `AgentEntitlement`（2 件、オンデマンド）**、シード用 Custom Resource Lambda（既存と共用）＋LogGroup×1 | ローカル：`out/tokens_userA-oidc.json`（V2-1 の agents/upn 入り）、`tokens_local-a.json`、退避 `tokens_userA-oidc_v1c.json`／`_session-reuse_v2.json` | ≈ $0.40（変わらず。DynamoDB は極小） |
| V2（Runtime 完了時） | | | |
| V3 | | | |
| V4 | | | |

## 4. 撤収手順（検証完了後。**実行は必ずユーザー確認のうえ**：§6）

### 4-1 事前確認
1. `docs/証拠マップ_v1_0.md` と各検証ログが揃っているか（消すと再取得できない実測が無いか）
2. `out/` のトークン・SAML 実物は検証ログにマスク済みで転記済みか → ローカルからは削除

### 4-2 CDK スタックの削除（依存の逆順）
```bash
cd CDK/agentcore-authchain/cdk
export AWS_PROFILE="Y_admin"
npx cdk list                              # スタック一覧を確認
npx cdk destroy <V4のPolicyを含むスタック>   # PolicyEngine／Gateway 紐付け → 先に外す
npx cdk destroy <Gateway/Runtime のスタック>
npx cdk destroy <Cognito/DynamoDB のスタック>  # UserPool は removalPolicy DESTROY 前提
```
- Cognito ドメインはプールと同じスタックなら一緒に消える。ドメイン削除は伝播に数分かかることがある
- DynamoDB は `removalPolicy: DESTROY` にしておく（検証用）。RETAIN のままだとテーブルが残る

### 4-3 消し忘れチェック（cdk destroy では消えないもの）
| 対象 | 確認コマンド／場所 | 対処 |
|---|---|---|
| S3 の CDK アセット（Runtime コード zip・Lambda zip。ブートストラップバケット `cdk-hnb659fds-assets-<acct>-ap-northeast-1` に残る） | `aws s3 ls s3://cdk-hnb659fds-assets-<acct>-ap-northeast-1/` | 放置可（極小）。掃除するなら `cdk gc`（要確認）。**兄弟プロジェクトの資産と同居しているので削除対象を絞る**。ECR は本検証では使わない（仮決め #13） |
| CloudWatch Logs グループ（Lambda 自動作成 `/aws/lambda/...`、AgentCore `/aws/bedrock-agentcore/...`、Cognito） | `aws logs describe-log-groups --log-group-name-prefix /aws/lambda/AuthChain` 等 | 削除 or 保持期間を短く（CDK 側で `LogGroup` を明示作成＋`RemovalPolicy.DESTROY` にしておけば destroy で消える） |
| Cognito ユーザープール（RETAIN の場合） | コンソール／`aws cognito-idp list-user-pools` | 手動削除 |
| DynamoDB テーブル（RETAIN の場合） | `aws dynamodb list-tables` | 手動削除 |
| 一時アプリクライアント（V3 の壊す実験） | `aws cognito-idp list-user-pool-clients` | V3 終了時に削除済みであること |
| Entra 側 | **SAML** エンタープライズアプリ `cognito-authchain-verify`（V1'-c で AWS 側は撤去済、Entra 側は残置）、**OIDC** の App registration（ランブック v2.0。クライアントシークレットも一緒に消える）、User-A/B、（専用に作ったなら）テナント | 削除。テナント削除は「ディレクトリの削除」（前提条件のクリアが必要） |
| Secrets Manager `authchain/entra-oidc-client-secret`（CDK 管理外。ランブック v2.0 §3 で手動作成） | `aws secretsmanager describe-secret --secret-id authchain/entra-oidc-client-secret` | `delete-secret --force-delete-without-recovery`（要確認）。残すと $0.40/月 |
| ローカル | `out/`（トークン・SAML）、一時 DOCKER_CONFIG、`docker image rm tonistiigi/binfmt`（任意）、`docker run --privileged --rm tonistiigi/binfmt --uninstall arm64`（任意） | 削除 |

### 4-4 撤収後の確認
- `aws cloudformation list-stacks --stack-status-filter CREATE_COMPLETE UPDATE_COMPLETE` に AuthChain 系が無い
- Cost Explorer で翌日以降 AgentCore／Cognito の課金がゼロに戻る

## 変更履歴
- v1.0（2026-08-16）下書き。単価（公式）、想定使用量と概算（$1 未満/月）、V 別稼働リソース表の枠、撤収手順（destroy 順・消し忘れチェック）を記載
- v1.0 追記（2026-08-16）V2 を直接コードデプロイに変更したため ECR 関連を S3 アセットに置き換え
- v1.0 追記（2026-08-16）V1a 時点の稼働リソース行を記入
- v1.0 追記（2026-08-16）V1 完了時の行に更新
- v1.0 追記（2026-08-30）V1' 完了時の行を追加（SAML IdP 撤去、Secrets Manager $0.40/月）。§4-3 に OIDC App registration と Secrets Manager を追加
- v1.0 追記（2026-08-30）V2-1（AgentEntitlement）の行を追加
