# 再開プロンプト（2026-09-06 セッション末尾の状態）

次回セッションの冒頭に、以下をそのまま貼り付けて開始する。

---

AgentCore 認証チェーン検証（`CDK/agentcore-authchain/`、ブランチ `feat/agentcore-authchain-v0`）の続きを行います。以下の状態から再開してください。

## 目的（2 軸）
1. 要件固有：組織属性の正規化 → エージェント認可マスタ → `agents` クレーム → Runtime 入口認証 → 同一 Bearer 透過 → Gateway Policy（Cedar）の連鎖を実測で確かめる
2. 汎用：AgentCore（Runtime／Gateway／Identity／Policy／Observability）で AI エージェントシステムを組む型を、**公式ベストプラクティスに沿って**作りながら理解する。要件固有でない部分はベストプラクティス準拠、検証の都合で緩めた箇所は「簡略化」と明記して本番案を併記（`docs/整理_システム全体像_v1_0.md` §5）

## 現在地
- **V1' 完了**（OIDC 化）、**V2-1 完了**（`AgentEntitlement`＋`agents`／`upn` 注入）、**V2 完了（2026-09-06）**：`AuthChainRuntimeStack` を deploy（V2a → V2b、現在は `forwardAuth=true` の状態＝Runtime version 2、`requestHeaderAllowlist: ["Authorization"]`）。壊す実験 4 種はすべて **401**（`検証ログ_V2` の表）。`AgentEntitlement` の一時削除行は復元済み
- 主な実測：MMDSv2 は**サービス既定で有効**（CFN 未対応でも `update-agent-runtime` 不要）／Runtime 入口の拒否はすべて 401（customClaims 不一致は "Authorization denied" と汎用文言）／refresh_token でも Pre Token Gen が走り `agents` が再計算される／CloudTrail（管理イベントのみ）に `InvokeAgentRuntime` は出ない／base64 末尾 1 文字の書き換えは改ざんにならない（`tamper()` を 1 ビット反転に修正済み）
- `runtime-code/agent.py` の `SHOW_CLAIMS` に `upn` を追加したが **`build/` 未再生成・未デプロイ**（V3 の deploy 前に `runtime-code/build.sh` を実行すると反映される）
- docs：`検証ログ_V2`（V2a／V2b／壊す実験／完了サマリ）、`仮決め事項表` #13/#15/#16、`コスト見積り` §3 V2 行、`整理_システム全体像` §1-2/§4/§5-(b)、`ツール使い方` §4、`計画` §6-2/4/8 を更新済み
- すべてコミット済み（`git log -1` で確認）。**origin へ未 push**（push 前に秘密スキャン：GUID／12 桁／PoolId／26 文字クライアント ID／メール／JWT／鍵）
- 稼働リソース：`AuthChainIdentityStack`（Cognito＋DynamoDB×2＋Lambda）、`AuthChainRuntimeStack`（Runtime＋実行ロール＋Workload Identity＋LogGroup）、Secrets Manager `authchain/entra-oidc-client-secret`（$0.40/月、期限 2027-02-25）。Runtime は invoke 従量で待機コスト無し
- ローカルの秘密（gitignore）：`out/entra-oidc.json`、`out/identity-outputs.json`、`out/runtime-outputs.json`、`out/tokens_userA-oidc.json`（refresh_token 入り。アクセストークンは 60 分で期限切れ → refresh グラントで更新可、`ツール使い方` §4）、`out/tokens_userA-oidc_refreshed.json`／`_noagents.json`、`out/runtime_v2*.json`（応答の現物 6 ファイル）、`out/entitlement_backup.json`

## 最初に読むもの
1. `docs/索引_v1_0.md`（§0 目的、§1 読む順、§3 V 終了時チェックリスト）
2. `docs/整理_システム全体像_v1_0.md`（§1-2 現在の構成、§4 認可 3 層、§5-(b) 未デプロイ部分の方針）
3. `docs/検証ログ_V2_v1_0.md`（V2a／V2b の実測、壊す実験の表、完了サマリ）
4. `docs/計画_追加要件_v1_0.md` §3-4（Gateway＋Policy）／§5／§6／§8-4
5. 必要に応じて `docs/ツール使い方_観測手順_v1_0.md` §4（`invoke_runtime.py --action gateway`、refresh グラント）

## 今回やること（この順で）
1. **V3 設計の確認**：`AuthChainGatewayStack`（Gateway JWT `usingCognito` `allowedClients`、Lambda ターゲット `VerifyTarget___echo_profile`、Policy 未接続）の CDK を書く前に、計画書 §3-4 と `整理_システム全体像` §5-(b)「Gateway インバウンド」「Gateway を前置」の方針を再確認。Gateway の vended logs（Observability）を入れるか判断
2. **V3 実装**：`cdk/lib/gateway-stack.ts`（新規）、`bin/authchain.ts` に追加、Runtime へ `-c gatewayUrl=<出力>` を渡す。`runtime-code/build.sh` を再実行（`upn` 表示の反映）→ `cdk diff`（予想 → 実測）→ deploy（IAM を作るので事前確認）
3. **V3 実測**：`invoke_runtime.py --action gateway`（`tools/list` でツール名の実物 `___`）→ `--tool VerifyTarget___echo_profile` → Lambda ターゲットに渡るユーザー情報（計画 §6-6）を Lambda ログで確認。壊す実験：期限切れトークンで Gateway 401 の文言
4. 検証ログ V3 を新規作成、仮決め #16 の実測、コスト表 V3 行、`整理_システム全体像` §1/§5-(b)、再開プロンプト → コミット
5. 以降 V4（Policy LOG_ONLY → ENFORCE）は計画書 §3-4・§8-4。V5（Strands＋モデル）は V4 後に検討

## 進め方のルール
- 核心ステップは「予想 → 実測 → 照合」（予想は Claude が代行可）。周辺ステップは質問せず進める。課金・削除・IAM・グローバル設定の変更は事前確認
- `export AWS_PROFILE="Y_admin"` は毎セッション確認。テストは書かない。一度に 1 変更。公開リポジトリのマスク規約（`<tenant>`/`<ACCOUNT>`/`<PoolId>`/`<ClientId>`/`<user>@<tenant>…`）
- 設計判断は「要件固有／汎用（ベストプラクティス準拠）／簡略化」を明示。公式推奨からの逸脱は理由付きで `整理_システム全体像` §5 に記録し、実装を変えるかは各 V の diff 提示時に個別確認
- 再ログインは `get_token.py --user userA-oidc --idp EntraOIDC`（`python3 -u` でバックグラウンド、停止は PID 指定、シークレットウィンドウ）。アクセストークンの更新だけなら refresh グラント（ブラウザ不要）

## 未確認事項（計画書 §6。残り）
Policy での配列クレーム（`agents`）の tag 表現／`CfnPolicy.enforcementMode` と policy 単位 `enforcementMode` の実動作／Gateway Lambda ターゲットに渡るユーザー情報／Cognito resource binding（`aud` 付き）と `allowedAudience`（余力枠）／AgentCore の CloudTrail データイベント対応（`InvokeAgentRuntime` を証跡に残す方法）

まず現状確認（`git status`、`git log --oneline -3`、`out/` の有無、`runtime-code/build/` の有無、`get-agent-runtime` で Runtime の状態）をして、V3 の設計方針と `cdk diff` の予想を提示してください。
